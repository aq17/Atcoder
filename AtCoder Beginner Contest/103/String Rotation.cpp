#include <bits/stdc++.h>
using namespace std;

int main(){
  string s, t;
  cin >> s >> t;
  
  string test;
  for (int i = 0; i < s.length(); i++){
    test += s.
  }
  for (int i = 1; i < s.length(); i++){
    
  }
}